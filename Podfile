# Instructions :
# In Project folder:
# > touch Podfile
# Then:
# < pod install

# If cocoaPods fail due to old version
# [sudo] gem update cocoapods

link_with ['SoundCheck', 'SoundCheckTests']

platform :ios, :deployment_target => '7.0'

pod 'CocoaSoundCloudAPI', '~> 1.0.2'
pod 'CocoaSoundCloudUI', '~> 1.0.7'
pod 'JSONModel', '~> 0.9.2'
